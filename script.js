const listContainer = document.getElementById('professionalsList'); const searchInput = document.getElementById('searchInput'); function displayProfessionals(professionalsToShow) { listContainer.innerHTML = ''; if (professionalsToShow.length === 0) { listContainer.innerHTML = '<p>No se encontraron profesionales.</p>'; return; } professionalsToShow.forEach(prof => { const card = document.createElement('div'); card.className = 'professional-card'; card.innerHTML = ` <img src="${prof.photo}" alt="Foto de ${prof.name}" class="professional-photo" /> <div class="professional-info"> <h2>${prof.name}</h2> <p><strong>Teléfono:</strong> ${prof.phone}</p> <p><strong>Servicios:</strong> ${prof.services}</p> <p><strong>Zona:</strong> ${prof.zone}</p> <p><em>"${prof.review}"</em></p> </div> `; listContainer.appendChild(card); }); } function filterProfessionals() { const query = searchInput.value.toLowerCase(); const filtered = professionals.filter(prof => { return ( prof.name.toLowerCase().includes(query) || prof.services.toLowerCase().includes(query) || prof.zone.toLowerCase().includes(query) ); }); displayProfessionals(filtered); } searchInput.addEventListener('input', filterProfessionals); // Mostrar todos al cargar la página
displayProfessionals(professionals);
